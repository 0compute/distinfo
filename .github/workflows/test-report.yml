name: Test Report

on:
  workflow_run:
    workflows:
      - Check
    types:
      - completed

permissions:
  contents: read
  actions: read
  checks: write

jobs:
  report:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test:
          - utest
          - ftest
          - atest
    steps:
      - uses: dorny/test-reporter@v1.6.0
        with:
          artifact: test-result
          name: ${{ matrix.test }}
          path: pytest-${{ matrix.test }}.xml
          reporter: java-junit
